# IRNumberHandle

```json
{
  "object_name": "IRNumberHandle",
  "type": "[IRType](../type)"
}
```

`type`

:   The type of the number field that the handle should be stored in.
    
    See [IRType](../type).
